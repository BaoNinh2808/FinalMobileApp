<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileAppFinal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- You can set a light parent theme -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@android:color/background_light</item>
        <!-- Add other light theme customizations as needed -->
    </style>

    <style name="Theme.MobileAppFinal" parent="Base.Theme.MobileAppFinal">
        <!-- Customize specific attributes for this theme if needed -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Add other theme-specific customizations as needed -->
    </style>
</resources>
